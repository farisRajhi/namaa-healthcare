/**
 * Run the Patient Intelligence pipeline on 500 patients and save detailed output.
 * Usage: node --loader ts-paths-esm-loader --import tsx __tests__/fixtures/run-500-test.cjs
 * Or simpler: just use the integration test approach via vitest
 */
// This is a marker file — the actual test runs via vitest below.
console.log('Use: npx vitest run __tests__/services/patientIntelligence/pipeline-500.integration.test.ts');
